// The specific part of the file we need to update on line 526 is:
// formatter={(value, name) => [`${formatNumber(typeof value === 'number' ? value : 0)} downloads (${downloadSources.find(s => s.name === name)?.percent}%)`, name]}